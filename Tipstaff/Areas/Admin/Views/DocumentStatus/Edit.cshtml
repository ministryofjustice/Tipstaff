@model Tipstaff.Models.DocumentStatus

@{
    ViewBag.Title = ViewBag.Title = string.Format("Edit document status {0}", Model.Detail);
    Layout = "~/Views/Shared/_Layout.cshtml";
}
    <h1>@ViewBag.Title</h1>
<h6>@Html.ActionLink("Administration", "Index", "Admin") 
> @Html.ActionLink("Lookups", "Lookups", "Admin") 
> @Html.ActionLink("Document statuses","Index") 
> @Html.ActionLink(Model.Detail, "Details", "DocumentStatus", new { id = Model.DocumentStatusID }, null)
> Edit</h6>

	
@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    @Html.AntiForgeryToken();
    <fieldset>
        <legend>@ViewBag.Title</legend>
        @Html.HiddenFor(model => model.DocumentStatusID)
        @Html.HiddenFor(model => model.active)

        <div class="editor-label">
            @Html.LabelFor(model => model.Detail, true)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Detail)
            @Html.ValidationMessageFor(model => model.Detail)
        </div>
        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

