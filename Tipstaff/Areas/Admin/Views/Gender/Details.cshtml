@model Tipstaff.Models.Gender

@{
    ViewBag.Title = string.Format("Details of {0}", Model.detail);
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h6>@Html.ActionLink("Administration", "Index", "Admin") 
> @Html.ActionLink("Lookups", "Lookups", "Admin") 
> @Html.ActionLink("Genders", "Index") 
> @Model.detail</h6>
<fieldset>
    <legend>@ViewBag.Title</legend>

    <div class="display-label">@Html.LabelFor(model => model.detail)</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.detail)
    </div>
    @if (Model.deactivated != null)
    {
    <div class="display-label">
        @Html.LabelFor(model => model.deactivated)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.deactivated)
    </div>

    <div class="display-label">
        @Html.LabelFor(model => model.deactivatedBy)
    </div>
    <div class="display-field">
        @Html.DisplayFor(model => model.deactivatedBy)
    </div>
    }
</fieldset>
<p>
    @if (Model.active == true)
    {
        @Html.ActionLink("Edit", "Edit", new { id=Model.genderID })
    }
</p>
